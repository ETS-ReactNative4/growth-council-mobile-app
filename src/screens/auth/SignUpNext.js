import React from 'react';

import SignUpNext from './components/SignUpNext';

const SignUpNextScreen = (props) => {

    return (
        <SignUpNext
            {...props}
        />
    );
};

export default SignUpNextScreen;
